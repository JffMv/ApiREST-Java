package co.edu.ing.escuela.proofparcial;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The ProofParcialApplication class is the entry point for starting the Spring Boot application.
 */
@SpringBootApplication
public class ProofParcialApplication {

	/**
	 * Main method to start the Spring Boot application.
	 *
	 * @param args command-line arguments (not used).
	 */
	public static void main(String[] args) {
		SpringApplication.run(ProofParcialApplication.class, args);
	}

}
